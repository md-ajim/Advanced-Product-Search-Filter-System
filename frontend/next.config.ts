import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["https://advanced-product-search-filter-system.onrender.com"],
  },
};

export default nextConfig;
